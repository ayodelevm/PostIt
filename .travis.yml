language: node_js
node_js:
  - "7"
services:
  - postgresql
before_script:
  - psql -c 'create database postit_auth;' -U postgres
  - psql -c 'create database postit_auth_test;' -U postgres
script: 
  - npm install
  - npm run test
  - npm run coverage
after_success:
  - npm run coveralls
